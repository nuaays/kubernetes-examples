apiVersion: v1
kind: ReplicationController
metadata:
  name: push-ios
  labels:
    name: push-ios
spec:
  replicas: 2
  selector:
    name: push-ios
  template:
    metadata:
      labels:
        name: push-ios
    spec:
      containers:
      - name: push-ios
        image: registry.test.com:5000/push-ios:v1
        ports:
        - containerPort: 8080
        resources:
          limits:
            cpu: "2"
            memory: "4096Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
            scheme: HTTP
          initialDelaySeconds: 120
        volumeMounts:
        - mountPath: /apps/log
          name: storage
      volumes:
      - name: storage
        source:
          emptyDir: {}
